content-scrapper
================

This piece of code will scrape the content form the site based on the html tag.

Change the tag according to the site from which ur going to extract. Inorder to iterate to all the sub pages iterate through
those pages by scrapping those numbers.
